# Needs attention

The importer places an image here only when that individual filename or image
cannot be published. Its adjacent `.error.txt` file explains the correction.

Fix or rename the image, then move it back into the matching
`imports/incoming/<player>/` folder. Temporary service failures never move an
image here; the entire workflow fails and leaves the original batch untouched.

